{
  "routes": [
    {
      "method": "GET",
      "path": "/lookups",
      "handler": "lookup.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/lookups/count",
      "handler": "lookup.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/lookups/:id",
      "handler": "lookup.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/lookups",
      "handler": "lookup.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/lookups/:id",
      "handler": "lookup.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/lookups/:id",
      "handler": "lookup.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
